# dsh-legion is an agent-plane plugin. DSH anchors a profile's Loader baseUrl at
# that profile directory, so installing this bundle makes profile-local
# node_modules/dsh-legion resolvable from user-authored agent presets. No Host
# row is needed or desired.
[]
